package core.demo;

/**
 * Created by Qtyearlin on 2017/2/7.
 */

public class rvadapter {
    /*RecyclerView recycleView = bindingView.recycleView;
    recycleView.setLayoutManager(new LinearLayoutManager(getContext()));
    BaseQuickAdapter adapterS = new BaseQuickAdapter<LeftMenuBean, BaseViewHolder>(R.layout.item_left_menu, menus) {
        @Override
        protected void convert(BaseViewHolder baseViewHolder, LeftMenuBean o) {
            baseViewHolder.setImageResource(R.id.imv_menu,o.getIcon()).setText(R.id.tv_menu,o.getTitel());
        }

    };
    BaseMultiItemQuickAdapter adapterM = new BaseMultiItemQuickAdapter<LeftMenuBean, BaseViewHolder>(menus) {

        @Override
        protected void convert(BaseViewHolder baseViewHolder, LeftMenuBean o) {

        }

    };
    View headView = LayoutInflater.from(getContext()).inflate(R.layout.layout_left_headview,null);
    adapterS.addHeaderView(headView);
    adapterS.setHeaderAndEmpty(true);
    View emptyView = LayoutInflater.from(getContext()).inflate(R.layout.core_layout_empty,null,true);
    adapterS.setEmptyView(emptyView);
    recycleView.setAdapter(adapterS);
    recycleView.addOnItemTouchListener(new OnItemChildClickListener() {
        @Override
        public void onSimpleItemChildClick(BaseQuickAdapter baseQuickAdapter, View view, int i) {

        }
    });*/
}
